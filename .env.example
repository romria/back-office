# Base environment — committed to source control.
# Contains safe defaults. Copy to .env for local overrides (gitignored).
#
# All client-side variables must be prefixed APP_ to distinguish them from build-tool variables (NODE_ENV, ANALYZE, etc.).

# API
APP_API_BASE_URL=https://dummyjson.com
